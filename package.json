{
  "name": "moon-proxy",
  "version": "0.0.1",
  "description": "Moon Proxy browser with server-side proxy support.",
  "engines": {
    "node": ">=18"
  },
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.12",
    "express": "^4.18.2"
  }
}
